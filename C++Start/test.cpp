#include <iostream>
#include <cstdio>
#include <vector>
#include <string>
#include <algorithm>
#include <bits/stdc++.h>

// #define ms(s,val) memset(s, val, sizeof(s))

using namespace std;

int main(int argc, char *argv[])
{

    // long long s = 10000000000000000;
    // char c = 'A';
    // int i = 10;
    // const char *st = "Sss";
    // printf("%010.2f        %-10.2f\n%10.2f %10.2f\n", fs, fs, fs, fs);
    // printf("%lld %s %c %d\n", s, st, c, i);
    
    
    
    return 0;
}
